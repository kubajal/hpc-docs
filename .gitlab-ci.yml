stages: 
  - test
  - deploy

test-pages:
  stage: deploy
  tags: 
    - www
  when: always
  script:
    workon mkdocs-env
    mkdocs build --verbose --clean --strict
  allow_failure: false 

publish-pages:
  stage: deploy
  tags: 
    - www
  when: always
  script:
    workon mkdocs-env
    mkdocs gh-deploy
  allow_failure: false 
